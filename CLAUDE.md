# OpenDelegate Claude instructions

Read and follow `AGENTS.md` before doing any work. Then read the required context
listed there. `CONTEXT.md` is the current source of truth. `README.md`, `AGENTS.md`,
and the project skills explain how to apply it.

The earlier `docs/PRODUCT_SPEC.md`, `docs/IMPLEMENTATION_PLAN.md`, and
`docs/DECISIONS.md` describe the retained Admin Web prototype. Read or modify them
only when the owner explicitly requests legacy prototype work.
