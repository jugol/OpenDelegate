interface:
  display_name: "Initialize OpenDelegate"
  short_description: "Set up an SSH-first Hermes Device fleet"
  default_prompt:
    "Use $opendelegate-init to configure this Origin and its SSH-reachable Hermes peers safely."
